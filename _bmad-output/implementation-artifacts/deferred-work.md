# Deferred Work

## Deferred from: code review of story-6.3 (2026-06-02)

- **Double appel `/auth/me` par page admin** — `SiteHeader` exécute son propre `getMe()` au mount sur toutes les routes (y compris `/admin/*`), en parallèle du garde de `AdminShell` → 2 requêtes `/auth/me` concurrentes par chargement de page admin, sans cache partagé. Pré-existant : `SiteHeader.tsx` n'est pas modifié par la Story 6.3. Mutualiser l'état d'auth (contexte/SWR) éliminerait le doublon. Non bloquant. [siana-memento-web/src/components/siana/SiteHeader.tsx:41]

## Deferred from: code review of story-6.2 (2026-05-31)

- **Sémantique coût API / marge brute (D1)** — Le coût API est agrégé sur **toutes** les générations de la fenêtre 30j tandis que revenu/commandes ne comptent que `paid` → une fois la Story 6.3 livrée (coût réel persisté), `grossMargin` peut devenir fortement négatif et `avgApiCost` (÷ commandes payées) ≈ coût/taux-de-conversion (~10€ à 5%). Le dashboard agrège globalement, le CSV par commande → totaux divergents. À statuer (garder « marge business 30j » vs coût par commande payée) quand le coût réel sera disponible en 6.3. Aujourd'hui sans impact (coût = 0 partout). [metrics_service.ts:67-84 ; admin_controller.ts:57]
- **Coût API réel non persisté** — Aujourd'hui `order.design.generations` est vide (`generation_service.ts` n'écrit pas les générations), donc le CSV affiche `Coût API = 0,00` et `Marge = Montant` sur chaque ligne, et le coût total dashboard est 0. La persistance réelle du coût Gemini relève de la **Story 6.3** ; rebrancher coût/marge dessus une fois 6.3 livrée. [admin_controller.ts:57 ; metrics_service.ts:67-82]
- **Index manquants sur `generations.created_at` et `designs.created_at`** — La migration `1775900000100` n'indexe que `orders(created_at, status)`, mais les agrégations métriques scannent aussi `generations` et `designs` par `created_at` (tables qui croissent plus vite). Ajouter les index correspondants pour tenir la cible NFR-SC3 (10K commandes). Non bloquant au MVP.
- **Export CSV en mémoire sans pagination** — `exportCsv` charge toutes les commandes de la période avec `preload('design.generations')` et construit le CSV en une string. À l'approche de 10K commandes : pic mémoire + requête lente. Passer en streaming/pagination si le volume grossit. Non bloquant au MVP.
- **Download CSV via `<a href>` sans gestion d'erreur** — Si la session expire entre le chargement de la page et le clic « Exporter CSV », la navigation `<a>` vers l'endpoint protégé affiche le JSON `{success:false}` brut au lieu d'un toast. L'approche `<a href>` est autorisée par la spec ; améliorer en `fetch→blob` avec gestion d'erreur si besoin. UX mineure. [AdminDashboard.tsx ; admin.ts]
